            const response = await github.request("PUT /orgs/{org}/teams/{team_slug}/repos/{owner}/{repo}", {
              org,
              team_slug,
              context.repo.owner,
              context.repo.repo,
              permission
            });

 
